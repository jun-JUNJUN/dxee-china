# This file is used by direnv to automatically activate the backend virtual environment
# when you navigate to this directory.
# To use this, you need to install direnv and add 'eval "$(direnv hook zsh)"' to your .zshrc

# Activate the backend virtual environment
source_up
source ./backend/venv/bin/activate

# Notify user
echo "Backend virtual environment activated via direnv."
echo "You are now using: $(which python)"
